#pragma once
#include <vector>
#include <string>

using std::vector;
using std::string;

class day2
{
public:
	static int run(vector<string> input, bool partB);
	static int partA(vector<string> input);
	static int partB(vector<string> input);
};