#pragma once
#include <vector>
#include <string>

using std::vector;
using std::string;

class day1
{
	public:
		static int run(vector<string> input, bool partB);
		static int partA(vector<int> input);
		static int partB(vector<int> input);
};

